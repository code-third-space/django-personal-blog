[client]
# 客户端连接配置
user = djangouser
password = django
host = localhost
database = djangodbl
default-character-set = utf8mb4

[mysqld]
# MySQL 服务器端配置
sql_mode = "STRICT_TRANS_TABLES"
transaction-isolation = READ COMMITTED

